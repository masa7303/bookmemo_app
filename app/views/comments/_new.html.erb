<%= form_with(model: [ @book, Comment.new ], local: true) do |form| %>
    お気に入りの文章<br>
    <%= form.text_area :body, size: '50x8' %><br>
    <%= form.date_select :posted_at, {:use_month_numbers => true }, class: 'comment_date' %>
    <%= form.submit %>
<% end %>
